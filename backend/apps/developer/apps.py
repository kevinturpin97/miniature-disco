"""App configuration for developer."""
from django.apps import AppConfig


class DeveloperConfig(AppConfig):
    """Django AppConfig for the developer app."""

    name = "apps.developer"
    label = "developer"
