"""App configuration for organizations."""
from django.apps import AppConfig


class OrganizationsConfig(AppConfig):
    """Django AppConfig for the organizations app."""

    name = "apps.organizations"
    label = "organizations"
