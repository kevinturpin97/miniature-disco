"""App configuration for compliance."""
from django.apps import AppConfig


class ComplianceConfig(AppConfig):
    """Django AppConfig for the compliance app."""

    name = "apps.compliance"
    label = "compliance"
