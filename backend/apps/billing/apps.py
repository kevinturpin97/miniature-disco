"""App configuration for billing."""
from django.apps import AppConfig


class BillingConfig(AppConfig):
    """Django AppConfig for the billing app."""

    name = "apps.billing"
    label = "billing"
