"""App configuration for marketplace."""
from django.apps import AppConfig


class MarketplaceConfig(AppConfig):
    """Django AppConfig for the marketplace app."""

    name = "apps.marketplace"
    label = "marketplace"
