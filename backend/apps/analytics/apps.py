"""App configuration for analytics."""
from django.apps import AppConfig


class AnalyticsConfig(AppConfig):
    """Django AppConfig for the analytics app."""

    name = "apps.analytics"
    label = "analytics"
