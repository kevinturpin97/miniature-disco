"""App configuration for schedules."""
from django.apps import AppConfig


class SchedulesConfig(AppConfig):
    """Django AppConfig for the schedules app."""

    name = "apps.schedules"
    label = "schedules"
